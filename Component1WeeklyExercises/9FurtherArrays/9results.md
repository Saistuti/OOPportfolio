# 9 Further Arrays
15 marks
**to be committed to GitHub by 20th April**
1 Histogram -- complete

2 LL Bank Accounts -- complete

3 Post Codes -- complete

4 Quiz Time -- complete

Please replace **todo** with **complete** when done.

Record your progress on the OOP Form on README.md.

---
For each question in the exercise, please either display the output generated by
running the program, or the answer if the task is a question.

1 - Histogram
Design and implement an application that creates a histogram that allows you to
visually inspect the frequency distribution of a set of values. The program should
read in an arbitrary number of integers that are in the range 1 to 100 inclusive; then
it should produce a chart similar to the following one that indicates how many input
values fell in the range 1 – 10, 11 – 20, and so on. Print one asterisk for each value
entered.
```
1 - 10 | ***
11- 20 | *
21- 30 | *****
31- 40 |
41- 50 | ******
51 -60 | ***
61- 70 | *
71- 80 | *****
81- 90 |
91 -100 | **
```
(4 marks)

// Histogram
```
import java.util.Scanner;

public class Histogram {
public static void main(String[] args) {
Scanner scanner = new Scanner(System.in);

        int[] ranges = new int[10]; // 10 ranges: 1-10, 11-20, ..., 91-100

        System.out.println("Enter integers between 1 and 100 (enter 0 to stop):");

        while (true) {
            int num = scanner.nextInt();

            if (num == 0) {
                break; // stop input
            }

            if (num >= 1 && num <= 100) {
                int index = (num - 1) / 10; // determines range
                ranges[index]++;
            } else {
                System.out.println("Invalid input. Enter number between 1 and 100.");
            }
        }

        // Display Histogram
        System.out.println("\nHistogram:");

        for (int i = 0; i < ranges.length; i++) {
            int start = i * 10 + 1;
            int end = (i + 1) * 10;

            System.out.printf("%2d -%3d | ", start, end);

            for (int j = 0; j < ranges[i]; j++) {
                System.out.print("*");
            }

            System.out.println();
        }

        scanner.close();
    }
}
```
---
Enter integers between 1 and 100 (enter 0 to stop):

34

55

67

45

10  

0

Histogram:

1 - 10 | *

11 - 20 |

21 - 30 |

31 - 40 | *

41 - 50 | *

51 - 60 | *

61 - 70 | *

71 - 80 |

81 - 90 |

91 -100 |

------------------------------------------------------------------------------

**2 - LL Bank Accounts
The L&L Bank can handle up to 30 customers who have savings accounts. Design and
implement a program that manages the customer accounts.
-Create an account
-Make a deposit
-Make a withdrawal
  Produce appropriate error messages for invalid transactions. Also provide a method to
  add 3 percent interest to all accounts when ever the method is called.
  Create a driver class to test all the methods.
(4 marks)**

// Account class
```
class Account {
private int AccountNumber;
private String name;
private double balance;

    // Constructor
    public Account(int AccountNumber, String name, double balance){
        this.AccountNumber = AccountNumber;
        this.name = name;
        this.balance = balance;
    }

    // Deposit
    public void deposit(double amount){
        if (amount > 0){
            balance += amount;
            System.out.println("Deposit successful.");
        } else {
            System.out.println("Invalid deposit amount.");
        }
    }

    // Withdraw
    public void withdraw (double amount) {
        if (amount <= 0) {
            System.out.println("Invalid withdrawal amount.");
        } else if (amount > balance) {
            System.out.println("Insufficient balance.");
        } else {
            balance -= amount;
            System.out.println("Withdrawal successful.");
        }
    }

    // Adding 3% Interest
    public void addInterest(){
        balance += balance * 0.03;
    }

    public void display(){
        System.out.println("Account No: " + AccountNumber + ", Name: " + name + ", Balance: " + balance);
    }
    public int getAccountNumber(){
        return AccountNumber;
    }
}

```
// Bank class
```
class Bank {
private Account[] accounts;
private int count;

    public Bank(){
        accounts = new Account[30];
        count = 0;
    }

    // Creating Account
    public void createAccount(int accNo, String name, double balance){
        if (count < 30){
            accounts[count++] = new Account(accNo, name, balance);
            System.out.println("Account created successfully.");
        } else {
            System.out.println("Bank is full. Cannot add more customers.");
        }
    }

    // Finding Account
    private Account findAccount(int accNo){
        for (int i = 0; i < count; i ++){
            if (accounts[i].getAccountNumber() == accNo){
                return accounts[i];
            }
        }
        return null;
    }

    //Deposit
    public void deposit(int accNo, double amount){
        Account acc = findAccount(accNo);
        if (acc != null){
            acc.deposit(amount);
        } else {
            System.out.println("Account not found.");
        }
    }

    // Withdraw
    public void withdraw(int accNo, double amount){
        Account acc = findAccount(accNo);
        if (acc != null){
            acc.withdraw(amount);
        } else {
            System.out.println("Account not found.");
        }
    }

    // Adding Interest to all accounts
    public void addInterestToAll(){
        for (int i = 0; i < count; i ++){
            accounts[i].addInterest();
        }
        System.out.println("3% interest added to all accounts.");
    }

    // Display All Accounts
    public void displayAll(){
        for (int i = 0; i < count; i ++){
            accounts[i].display();
        }
    }
}
```

// BankDriver Driver class
```
import java.util.Scanner;

public class BankDriver {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);
Bank bank = new Bank();

        int choice;

        do {
            System.out.println("\n--- L&L Bank Menu ---");
            System.out.println("1. Create Account");
            System.out.println("2. Deposit");
            System.out.println("3. Withdraw");
            System.out.println("4. Add Interest (3%)");
            System.out.println("5. Display All Accounts");
            System.out.println("6. Exit");

            System.out.print("Enter choice: ");
            choice = sc.nextInt();

            switch (choice) {
                case 1:
                    System.out.print("Enter Account No: ");
                    int accNo = sc.nextInt();
                    sc.nextLine(); // clear buffer

                    System.out.print("Enter Name: ");
                    String name = sc.nextLine();

                    System.out.print("Enter Initial Balance: ");
                    double bal = sc.nextDouble();

                    bank.createAccount(accNo, name, bal);
                    break;

                case 2:
                    System.out.print("Enter Account No: ");
                    accNo = sc.nextInt();

                    System.out.print("Enter Amount: ");
                    double dep = sc.nextDouble();

                    bank.deposit(accNo, dep);
                    break;

                case 3:
                    System.out.print("Enter Account No: ");
                    accNo = sc.nextInt();

                    System.out.print("Enter Amount: ");
                    double wit = sc.nextDouble();

                    bank.withdraw(accNo, wit);
                    break;

                case 4:
                    bank.addInterestToAll();
                    break;

                case 5:
                    bank.displayAll();
                    break;

                case 6:
                    System.out.println("Exiting...");
                    break;

                default:
                    System.out.println("Invalid choice.");
            }

        } while (choice != 6);

        sc.close();
    }
}
```
---
--- L&L Bank Menu ---

1. Create Account

2. Deposit

3. Withdraw

4. Add Interest (3%)

5. Display All Accounts

6. Exit

   Enter choice: 1

   Enter Account No: 123456

   Enter Name: John Brook

   Enter Initial Balance: 350000

   Account created successfully.
---
--- L&L Bank Menu ---

1. Create Account

2. Deposit

3. Withdraw

4. Add Interest (3%)

5. Display All Accounts

6. Exit

   Enter choice: 2

   Enter Account No: 123456

   Enter Amount: 4500

   Deposit successful.
---
--- L&L Bank Menu ---

1. Create Account

2. Deposit

3. Withdraw

4. Add Interest (3%)

5. Display All Accounts

6. Exit

   Enter choice: 3

   Enter Account No: 123456

   Enter Amount: 3200

   Withdrawal successful.
---
--- L&L Bank Menu ---

1. Create Account

2. Deposit

3. Withdraw

4. Add Interest (3%)

5. Display All Accounts

6. Exit

   Enter choice: 4

   3% interest added to all accounts.
---
--- L&L Bank Menu ---

1. Create Account

2. Deposit

3. Withdraw

4. Add Interest (3%)

5. Display All Accounts

6. Exit

   Enter choice: 5

   Account No: 123456, Name: John Brook, Balance: 361839.0
---
--------------------------------------
**3 - Post Codes
Design and implement and application that reads a sequence of up to 25 pairs of names
and postal codes for individuals. Store the data in an object designed to store a
first name, last name and postal code(all as strings). Assume that each line of input
will contain three strings, each separated by a tab character. After the input has
been read, print the list in an appropriate format to the screen.
(4 marks)**

// Person class
```
class Person {
private String firstName;
private String lastName;
private String postalCode;

    public Person(String firstName, String lastName, String postalCode) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.postalCode = postalCode;
    }

    public void display() {
        System.out.printf("%-15s %-15s %-10s\n", firstName, lastName, postalCode);
    }
}
```
// PostCodes Driver class
```
import java.util.Scanner;

public class PostCodes {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);

        Person[] list = new Person[25];
        int count = 0;

        System.out.println("Enter up to 25 records:");
        System.out.println("(FirstName<TAB>LastName<TAB>PostalCode)");
        System.out.println("Type 'exit' to stop input.");

        while (count < 25) {
            String input = sc.nextLine();

            if (input.equalsIgnoreCase("exit")) {
                break;
            }

            String[] parts = input.split("\t");

            if (parts.length != 3) {
                System.out.println("Invalid format! Use TAB between values.");
                continue;
            }

            list[count] = new Person(parts[0], parts[1], parts[2]);
            count++;
        }

        // Display output
        System.out.println("\n--- List of Individuals ---");
        System.out.printf("%-15s %-15s %-10s\n", "First Name", "Last Name", "Postal Code");
        System.out.println("---------------------------------------------");

        for (int i = 0; i < count; i++) {
            list[i].display();
        }

        sc.close();
    }
}
```
---
Enter up to 25 records:

(FirstName<TAB>LastName<TAB>PostalCode)

Type 'exit' to stop input.

Joe	Bill	223344

exit
---
--- List of Individuals ---

First Name      Last Name       Postal Code

---------------------------------------------

Joe             Bill            223344    

-----------------------------------------------------------------

**4 - Quiz Time
Define a class called Quiz that manages a set of up to 25 Question objects. Define the
add method of the Quiz class to add a question to the quiz. Define the giveQuiz method
of the Quiz class to present each question in turn to the user, accept an answer for
each one, and keep track of the results. Define a class called QuizTime with a main
method that populates a quiz , presents it , and prints the final results
(3 marks)**

// Quiz
```
class Quiz {
private Question[] questions;
private int count;

    public Quiz() {
        questions = new Question[25]; // max 25 questions
        count = 0;
    }

    // Add question
    public void add(Question q) {
        if (count < 25) {
            questions[count++] = q;
        } else {
            System.out.println("Quiz is full!");
        }
    }


    public void giveQuiz() {
        int score = 0;

        for (int i = 0; i < count; i++) {
            boolean correct = questions[i].askQuestion();

            if (correct) {
                System.out.println("Correct!\n");
                score++;
            } else {
                System.out.println("Wrong!\n");
            }
        }


        System.out.println("Quiz Finished!");
        System.out.println("Your Score: " + score + " / " + count);
    }
}
```
// QuizTime Driver class
```
public class QuizTime {
public static void main(String[] args) {

        Quiz quiz = new Quiz();

        // Add questions
        quiz.add(new Question("What is the capital of Nepal?", "Kathmandu"));
        quiz.add(new Question("What is 5 + 3?", "8"));
        quiz.add(new Question("Java is a programming language? (yes/no)", "yes"));


        quiz.giveQuiz();
    }
}
```
---
What is the capital of Nepal?

Your answer: Kathmandu

Correct!

---

What is 5 + 3?

Your answer: 8

Correct!

---
Java is a programming language? (yes/no)

Your answer: yes

Correct!

---
Quiz Finished!

Your Score: 3 / 3
